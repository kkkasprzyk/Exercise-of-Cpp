#include <iostream>
#include <cstring>
using namespace std;


class pracownik
{
public:
  char nazwisko_imie[96];
  long id;
  float pensja;
  int staz;
  void info (void)
  {
    cout << "Imie i nazwisko: " << nazwisko_imie << endl;
    cout << "Identyfikator: " << id << endl;
    cout << "Pensja: " << pensja << endl;
    cout << "Staz pracy: " << staz << endl;
  };
};

int
main (void)
{

  pracownik kierownik, kasjerka;

  strcpy (kierownik.nazwisko_imie, "Zbyszek Lepki");
  kierownik.id = 11882;
  kierownik.pensja = 9600;
  kierownik.staz = 14;

  strcpy (kasjerka.nazwisko_imie, "Eukasz Wieerzak");
  kasjerka.id = 1001000;
  kasjerka.pensja = 2700;
  kasjerka.staz = 3;

  kierownik.info ();
  kasjerka.info ();		//uzycie funkcji wyswietlajacowypisujacej

}

